{
    "name": "weather-oracle-subgraph",
    "license": "UNLICENSED",
    "scripts": {
        "codegen": "graph codegen",
        "build": "graph build",
        "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ weather-oracle-subgraph",
        "create-local": "graph create --node http://localhost:8020/ weather-oracle-subgraph",
        "remove-local": "graph remove --node http://localhost:8020/ weather-oracle-subgraph",
        "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 weather-oracle-subgraph"
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "0.98.1",
        "@graphprotocol/graph-ts": "0.36.0"
    }
}